<%= form_for(@company) do |f| %>
  <% if @company.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@company.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @company.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :company_nameantonio %>
    <%= f.text_field :company_name %>
  </div>

  <div class="field">
    <%= f.label :rif %>
    <%= f.text_field :rif %>
  </div>

  <div class="field">
    <%= f.label :status %>
    <%= f.text_field :status %>
  </div>

    <div class="field">
    <%= f.label :type_company %>
    <%= f.text_field :type_company %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
